# Compatibility

## Platforms
- Debian
- Ubuntu

## Requirements
- `apt-get`, `apt-cache`, and `dpkg-query` must be available in `PATH`.
- `sudo` is required for operations that modify the system when not running as root.
